# 无限画布项目手册

## 规则归属说明（2026-06-25）

- 本项目的生图逻辑、参考图路由、提示词重匹配、海报文案、相机控制、资产保留、空节点清理等维护规则，统一写在本项目的 `项目手册.md` 和 `AGENTS.md` 中。
- `C:\Users\pc\.codex\skills\start-infinite-canvas\SKILL.md` 只作为本地无限画布的启动、检查、重启、安全清理入口说明，不承载本项目的生图业务规则。
- 后续新增或修正规则时，优先写入本项目手册；只有启动脚本、端口、可清理文件范围这类跨任务通用操作，才允许放在 `start-infinite-canvas` 技能说明里。

## 2026-06-25：新生成图底部提示词卡片规则（已验证）

- 新生成图片被选中后，底部选项卡必须切换为与提示词节点一致的提示词编辑卡片，而不是直接生图参数卡片。
- 当前这张新生成图片默认作为后续修改的编辑基准：结构权重默认 `100%`，但必须允许用户手动调整。`100%` 时按严格编辑基准执行；调低后按当前权重放松结构锁定，不能在代码里再次强制写死为 `100%`。
- 原生成任务携带的产品细节图继续作为严格 SKU 事实图，不得被素材库案例图替换。
- 用户在 LLM 修改框中填写新需求后点击“运行”，必须使用 GPT-5.5 重新匹配素材库并生成新的完整提示词，随后直接替换当前卡片里的原提示词。
- “运行”只更新提示词，不自动生图、不覆盖当前生成图、不创建新的输出图。用户自行从该生成图节点拖出/连接图片节点，再手动执行后续生图。
- 相机控制与提示词节点保持同一套交互。启用后，当前焦段、水平机位和高度/俯仰参数必须随重匹配请求发送，GPT-5.5 不得擅自改写这些相机硬约束。
- 从该生成图节点向下游传递时，必须同时传递：新提示词、当前生成图编辑基准及其当前权重、产品事实图和当前相机控制。
- 当同一个目标节点连接多张“已生成图片”时，参考图缩略图和最终生图请求必须保留所有有效连接图。第一张作为主编辑基准；第二张及以后作为普通辅助生成参考，不得被多编辑基准去重逻辑直接丢弃，也不得同时抢占严格结构锁。
- 空上传节点只要存在真实输入/输出连线，就是用户正在使用的参考图汇合节点，保存和加载清理时必须保留；只有完全无图片、无任务、无手动参考、无任何连线的孤立空节点才允许自动清理。
- “海报文案”开关必须沿连接链传递到下游图片节点。下游节点不能因为自身没有显示该开关，就丢失上游提示词节点/生成图编辑卡片的开启状态。
- 开启“海报文案”后，主参考图或当前生成图编辑基准必须带 `posterCopyReference / copyTextLock` 并以高保真上传；最终生图提示词必须包含最高优先级 `POSTER COPY LOCK`。产品事实图只能控制 SKU，不能压掉主参考图里的标题、口号、徽章和文案层级。
- 如果当前生成图编辑卡片上明确关闭“海报文案”，则该关闭状态优先于更上游的开启状态。
- 品牌 Logo 和品牌名不是“海报文案”的一部分，必须单独视为品牌识别锚点。无论海报文案开关开/关，顶部品牌区 Logo、品牌名、产品线标识、活动角标，以及产品表面的品牌 Logo/标签都要尽量保留在原相对位置，不能被当作可删除文案。
- 关闭“海报文案”只表示移除/弱化营销文案，不表示清空品牌识别。必须保留海报品牌区 Logo、品牌名、产品线标识、店铺/活动品牌角标，以及产品表面的品牌 Logo、标签、规格和包装印刷图形。需要移除的是大标题、促销口号、价格、CTA、促销徽章、横幅、外部水印和无关装饰文字；被移除区域用留白、不可读短标记或简单图形块保持版式结构。
- 避坑：不要把“去海报文案”写成 `no readable text / no brand text / remove all logos` 这类泛化负面词，否则会误伤品牌 Logo、产品标签和顶部品牌区；提示词中应明确区分“品牌识别保留”和“营销文案移除”。
- 当前生成图底部再次点击“运行”时，必须以当前生成图为新的视觉基准重新分析，再结合用户的新需求、提示词库和产品事实图重写提示词；不能只沿用旧的 `styleMatch.analysis`。
- 如果最新需求包含“人物互动、使用场景、脚、双脚、两只脚、脚掌、腿、膝盖、踩、踩在、踩踏、踏板接触”等人体/肢体/接触意图，下游生图封装必须允许人物和肢体，不得再注入 `NO HUMAN SUBJECT LOCK`。
- 打开智能画布时必须刷新 `smart-canvas.html` 版本，防止浏览器缓存旧 HTML 导致新卡片容器缺失。

## 2026-06-25：生图逻辑改动前评估规则

- 改动任何一个点时必须有全局意识：先核对本手册、`AGENTS.md`、最近已验证修复记录和相关测试，确认当前修改不会破坏之前已经确定好的行为。
- 不能为了修当前问题，回退或削弱已有的关键约束，包括但不限于：参考图角色传递、产品 SKU 保真、无人物/允许人物判断、海报文案与品牌保留、相机控制、输出尺寸、空节点清理和任务生命周期。
- 如果当前修复与既有规则存在冲突，必须先停下来分析冲突来源，并向用户说明取舍；不能静默覆盖旧规则。
- 新增逻辑必须尽量做窄触发条件，只影响本次问题对应的场景；默认行为、已确认路径和无关生图内容不得被顺手改动。
- 修改后除了跑通校验，还要主动复查“本次改动是否影响之前确定好的点”，必要时补回归测试，把已确认规则固定下来。
- 不要随意修改生图逻辑和生图参数。凡是涉及参考图路由、权重、上传规则、提示词拼接、相机控制、输出尺寸、API payload、任务生命周期、重试/覆盖规则的改动，都必须先按 `AGENTS.md` 备份。
- 动手改代码前必须先核对当前链路：这次改动会不会影响已有的生图效果、参考图权重、产品图保真、人物/相机/背景约束、生成数量或输出节点覆盖行为。
- 如果评估后确认“默认行为会改变”或“现有生图结果可能受影响”，必须先把影响范围反馈给用户，让用户决定是否继续。
- 如果改动只影响用户主动开启/手动调整后的新行为，默认状态不变，也要在说明里讲清楚“默认不变，触发条件是什么”。
- 改完必须跑生成逻辑验证、前端语法检查、后端 Python 编译检查和 `git diff --check`，并在必要时重启本地服务验证页面实际行为。

## 2026-06-24：提示词节点“运行”重匹配与参考图传递规则（已验证）

这套规则来自 2026-06-24 的实测修复，当前用户确认“逻辑对了”。后续修改智能画布、生图链路、提示词节点或参考图路由时，必须保留这套行为，不能回退成自动生图或丢失参考图关系。

### 1. 提示词节点点击“运行”的职责

- “运行”只负责根据当前 LLM 修改框里的新需求，调用 GPT-5.5 重新匹配素材库，并生成一版新的完整提示词。
- 新运行得到的提示词必须直接替换当前提示词节点里的原提示词。
- 点击“运行”不能自动生图，不能自动创建/覆盖下游图片结果；用户会自己拖出或连接图片节点再手动生图。
- 当前节点里的参考图和产品图必须保持不变；运行只改文字，不替换、不清空、不重新选择参考图。

### 2. 当前参考图的固定角色

当一个匹配提示词/LLM 提示词节点同时带有多张当前参考图，并且文本里是产品相关需求时，默认采用以下角色分配：

- 图1：主参考图 / 场景结构 / 风格参考。用于保留人物关系、场景调性、灯光、背景色、商业质感、构图家族和画面关系。
- 图2 及以后：严格产品事实图。用于锁定 SKU、外形比例、结构部件、材质、颜色、表面质感、标签位置和真实产品几何。
- 素材库重新匹配出来的案例只能补充商业质感、灯光、纹理、风格 polish，不能替代图1的主参考结构，也不能替代图2+的产品事实。

### 3. 后续手动生图时的传递要求

- 用户从该提示词节点拖出/连接图片节点后再生图时，必须继续带上当前这些参考图。
- 最终进入生图请求的参考图排序应优先保留：1 张主参考图 + 最多 4 张产品事实图。
- 图1不能被误判为产品图后丢失场景作用；图2+不能被降级成弱风格图。
- 如果用户第二次、第三次点击“运行”更新提示词，不能把第一次生成的图片顶掉；提示词运行仍然只改当前提示词文本。

### 4. 最新需求优先于旧提示词约束

- LLM 修改框里的最新需求是最高优先级。
- 如果最新需求写了“人物、真人、模特、手持、拿着、握着、使用场景、使用效果、上脸、护理场景”等，必须允许人物/手部/真人使用场景出现。
- 如果最新需求写了“脚、双脚、两只脚、脚掌、腿、膝盖、踩、踩在、踩踏、人物互动、产品互动、踏板接触”等，必须同样视为明确的人物/肢体互动需求。
- 旧提示词里的 product-only、no-human、纯产品、无人物等约束，在这种情况下必须被覆盖或移除，不能继续压制最新的人物使用场景需求。
- 如果最新需求明确写“不要人物 / 无手 / 纯产品”，则继续启用无人物锁。
- 如果最新需求明确要求“改变背景、换背景、改变配色、调整色调”等，旧参考分析里的背景颜色、渐变、配色、服装/道具颜色锁必须清除；参考图只保留不冲突的人物关系、产品使用逻辑、光线质量、材质和商业质感。

### 4.1 相机控制与多图变体

- 启用相机控制后，焦段、水平机位、相机高度/俯仰、透视和裁切家族是不可被 LLM 重匹配或多图变体覆盖的硬约束。
- “生成 2 张不同动作角度”“不同姿势”“多种动作”等需求，在相机控制开启时只能变化人物动作、手势、表情、产品接触位置和画面内摆位。
- 多图变体不得擅自追加三分之二侧面、动态斜角、提高机位、降低机位、换焦段或不同 camera angle。
- 二次点击“运行”重新匹配提示词时，前端必须把当前相机控制作为独立字段传给 GPT-5.5；返回的新提示词不能改变当前 UI 选择的相机参数。

### 5. 维护注意

- 修改这块逻辑前仍需按 AGENTS.md 先备份生成逻辑。
- 修改后必须验证：
  - 点击“运行”只替换当前提示词；
  - 参考图缩略图保持不变；
  - 后续手动生图仍然使用图1主参考、图2+产品事实图；
  - 素材库案例不会把当前参考结构抢走；
  - 有“人物使用场景”新需求时，不会被旧的无人物约束拦截。

本文档记录本地 `C:\Users\pc\Documents\无限画布` 项目的核心维护规范，尤其是智能画布、生图链路、参考图权重和运行状态清理规则。后续改代码时优先遵守这里的约定。

## 运行与安全边界

- 本地服务入口：`http://127.0.0.1:3000/`。
- 首选启动/重启脚本：`C:\Users\pc\.codex\skills\start-infinite-canvas\scripts\manage-infinite-canvas.ps1`。
- 不要删除用户数据目录：`data/`、`assets/`、`output/`、`history.json`。
- 递归清理前必须确认目标路径；只清理日志、缓存、临时文件等可再生内容。
- 重启脚本可能会把 `static/smart-canvas.html` 里的 `smart-canvas.js?v=...` 改回时间戳。每次重启后如果改了前端 JS，要重新检查并更新 cache bust 版本号。

## 智能画布生成链路核心规则

### 修改生图逻辑前必须备份

每次修改参考图路由、人物相似度、产品约束、提示词压缩、相机覆盖、API 请求参数、
生成任务状态或空白节点清理之前，必须先运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\backup-generation-logic.ps1 -Reason "本次修改说明"
```

备份保存在 `backups/generation-logic/时间戳/`，包含 `main.py`、
`static/js/smart-canvas.js`、`static/smart-canvas.html`、项目手册和 SHA256 清单。
禁止覆盖或删除已有备份。

### 1. 输出节点状态必须完整闭环

生成输出节点只能处于以下几类状态之一：

- 等待中：`pending > 0` 或 `pendingTasks.length > 0` 或 `jimengPending`。
- 成功：`images` 中至少有一个真实 `url`，并且 `runFinishedAt` 有值。
- 失败/取消：必须清掉 `pending`、`pendingTasks`、`running`、`queued` 和运行元数据。

禁止留下这种“幽灵输出节点”：

- 没有图片；
- 没有 pending task；
- 不是 running；
- 没有 `runFinishedAt`；
- 但仍保留 `runPrompt`、`runModelPrompt`、`sourceNodeId`、`runAt` 或 `runStartedAt`。

这类节点会在画布上表现为空白卡片、错误计时、连接线残留。前端已通过 `filterStaleEmptyRunPlaceholders` 在加载时清理，但新代码仍要避免制造它们。

异步任务恢复/续查也必须即时收口：

- 如果任务失败但返回了可恢复的上游任务 ID，保留 `pendingTasks[].failed/recoverTaskId`，允许用户手动“查询结果”。
- 如果任务失败且没有任何图片、没有可恢复任务 ID，必须当场调用失败清理逻辑；自动分支输出节点应直接移除并写入 tombstone，避免被协作同步再次复活。
- `resumeSmartPendingTasks()` 是后台恢复流程，不能产生未处理的 Promise 异常；失败应被捕获并保存清理后的画布状态。
- `runGeneration()` 必须先确认存在选中节点，再构建 prompt 请求，避免空选择状态抛出前端异常。

### 2. `sourceNodeId` 不能指向自己

`sourceNodeId` 只表示“这个输出来自哪个上游源节点”。如果目标节点就是源节点本身，不能写入 `sourceNodeId`。

正确逻辑：

- 分支输出节点：`sourceNodeId = 源节点 id`。
- 原节点直接生成：不写 `sourceNodeId`。
- 输出节点失败并且无图片：调用失败清理逻辑，删除运行元数据。

### 3. 多图/二轮生成注意事项

当底部数量为 `2张`、`3张` 等多任务时：

- `pending` 初始值应等于任务数；
- 每个任务成功后只递减一次；
- 所有任务完成后必须删除 `pendingTasks`，写入 `runFinishedAt`；
- 若任务失败且节点没有图片，应清理节点或删除分支输出节点；
- 不允许把失败空节点继续作为下一轮可复用输出。

`reusableAutoOutputForSource` 只能复用已经完整完成且可显示的输出节点，不能复用空运行占位。

## 参考图与产品约束规范

### 1. 参考图角色

- 产品图：作为严格 SKU 事实参考，允许上传，优先约束产品结构、材质、颜色、标签位置、比例和细节。
- 人像/风格图：默认弱参考。中低权重时尽量转为文字风格参考，不上传原图，避免人物脸完全复制。
- 模板库匹配图：只用于提示词/风格描述，任何权重和场景下都不作为 API 图片参考上传。

### 1.1 人物/肢体出现权限

生成链路默认是“产品/物体优先”，不能因为案例库、风格库、旧提示词模板里出现 `model`、`hand placement`、`skin treatment`、`pose` 等词，就自动生成真人、手、脸或身体。

只有满足以下任一条件，才允许出现人物、手、脸、皮肤、肢体或人体剪影：

- 真实上传/连接的风格参考图经分析明确包含人物或肢体；
- 用户当前提示词、提示词节点或上游文本中明确要求人物、模特、手持、手拿、肢体、佩戴、真人场景等；
- 用户明确要求复刻/保留有人物的生活方式场景。

否则必须追加 `NO HUMAN SUBJECT LOCK`：

- 不出现 person / people / human / model / portrait / face / skin / hair / hand / fingers / arms / body / silhouette / mannequin；
- 产品可通过支架、台面、道具、机械结构、光影、粒子、环境来承托；
- 纯产品场景不能为了“手持关系”自动加入手。

### 2. 人物相似度

参考权重控制的是“风格/构图/人物类型相似度”，不是默认复制同一个人。

- 低权重：新人物、新姿势、新构图。
- 中权重：同商业风格，但人脸必须明显不同。
- 高权重：可更接近参考，但仍不要默认完全同脸，除非用户明确要求锁定人物。

### 2.1 参考图上传尺寸与性能

- 产品真值图最长边最多 `1536`，保留结构、材质和标签细节。
- 普通人物/构图/风格参考在权重 `<85` 时最长边最多 `768`。
- 高权重人物/构图/风格参考在权重 `>=85` 时最长边最多 `1024`。
- 模板案例图永不上传，后端必须无条件过滤 `case_style_reference`，防止案例图片意外进入请求。
- OpenAI multipart 编辑请求需要缩放时，只生成 `sized_ref_` 临时副本；不得修改或覆盖画布中的原始文件。

### 3. 产品手持关系

如果用户要求产品被拿着，提示词必须强调真实物理接触：

- 产品必须在手中；
- 至少两个手指或掌心与产品产生遮挡/接触；
- 不允许产品漂浮在手边；
- 手、产品、阴影、遮挡关系要合理。

## 尺寸/比例约束规范

底部尺寸选择是最高优先级之一。

- 用户选择 `9:16 · 2K` 时，最终输出必须按目标画布比例生成/保存。
- 参考图比例不能覆盖用户选择的输出比例。
- 老提示词里的 `Preserve its aspect ratio`、`strict composition` 等内容必须在运行前被弱化或覆盖。
- 前端会追加 `OUTPUT CANVAS LOCK`，告诉模型按用户选择的画布生成。
- 后端保存图片时会做兜底比例校正：如果上游返回比例不对，会本地转成请求尺寸。优先扩展/模糊背景，不硬裁产品和手部。
- `auto` 模式不强制比例。
- 异步生图任务、手动查询上游任务结果、同步返回结果都必须把请求 `size` 传给保存函数；否则第二轮/续查结果会绕过比例兜底。

## 提示词长度与压缩

提示词太长时，压缩必须保留这些高优先级段落：

- 用户最新修改要求；
- 产品事实/产品替换规则；
- `OUTPUT CANVAS LOCK`；
- 人物身份控制；
- 手持产品交互规则；
- 上传图片角色映射；
- 最终产品检查。

模板库、案例库、风格解释可以压缩；产品事实和输出画布约束不能被压掉。

## GPT-Image2 风格库集成规范

项目内置风格库目录：

```text
style_libraries/gpt-image-2/
```

后端解析顺序：

1. 如果设置了环境变量 `STYLE_LIBRARY_DIR`，优先使用该目录。
2. 否则优先使用项目内 `style_libraries/gpt-image-2/`。
3. 如果项目内库不存在，再回退到本机 Codex skill：`~/.codex/skills/gpt-image-2-style-library`。

这样上传到 GitHub 后，另一台电脑只要拉取本项目，就能继续使用画布里的“匹配提示词 / 风格案例检索”能力，不依赖当前电脑的 Codex skill 安装状态。

纳入仓库的内容只包含运行所需的检索脚本和参考库：

- `scripts/search_all.py`
- `references/*.json`
- `references/*.md`

不要提交风格库的日志、daily update state/cache、Codex agent 配置、本机私有 skill 目录、API Key 或任何用户输出文件。

### 自动更新

无限画布服务启动时会后台检查项目内风格库是否今天已更新：

- 如果 `style_libraries/gpt-image-2/references/daily-update-state.json` 的 `last_check_date` 是今天，则跳过；
- 如果今天未更新，则后台运行 `style_libraries/gpt-image-2/scripts/update_daily_sources.py`；
- 更新日志写入 `style_libraries/gpt-image-2/references/daily-update.log`；
- 锁文件为 `style_libraries/gpt-image-2/references/.daily-update.lock`；
- 网络失败时继续使用本地缓存，不影响画布启动和生图；
- 可用环境变量 `STYLE_LIBRARY_AUTO_UPDATE=0` 关闭；
- 可用 `STYLE_LIBRARY_AUTO_UPDATE_TIMEOUT=秒数` 调整最长等待时间。

自动更新只操作项目内 `style_libraries/gpt-image-2/`，不会写入 `C:\Users\pc\.codex\skills\gpt-image-2-style-library`，因此本机 Codex skill 不受影响。

## 前端验证规范

每次改 `static/js/smart-canvas.js` 或生成链路后，至少执行：

```powershell
node .\tools\verify-generation-logic.js
node --check static\js\smart-canvas.js
.\python\python.exe -m py_compile main.py
git diff --check -- static/js/smart-canvas.js main.py static/smart-canvas.html
```

重启后检查：

```powershell
powershell -ExecutionPolicy Bypass -File C:\Users\pc\.codex\skills\start-infinite-canvas\scripts\manage-infinite-canvas.ps1 status
```

浏览器 QA 要确认：

- 页面标题为“智能画布”；
- `composer`、`dynamicParams`、`runBtn` 存在；
- 控制台无关键 error/warn；
- 加载的 `smart-canvas.js?v=...` 是最新版本。

## 本次关键修复记录（2026-06-24）

- 修复二轮/多图生成后可能残留空白输出卡的问题。
- 新增空运行占位清理：`filterStaleEmptyRunPlaceholders`。
- `attachRunMeta` 不再把 `sourceNodeId` 写成节点自己。
- 失败且无图片时清理运行元数据，避免空节点被保存成有效输出。
- 清理当前画布中两个历史幽灵节点，并自动备份到 `data/canvas_backups/`。
- 输出比例新增双保险：前端 `OUTPUT CANVAS LOCK` + 后端保存时比例校正。
